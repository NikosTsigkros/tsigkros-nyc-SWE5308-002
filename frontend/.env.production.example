# Copy to .env.production locally, or set the same key in DigitalOcean
# (Static Site / Web Service → Environment → Build-time / Build command env).
#
# Use your real backend public URL, including /api (no trailing slash after /api).
VITE_API_URL=https://your-backend-app.ondigitalocean.app/api
